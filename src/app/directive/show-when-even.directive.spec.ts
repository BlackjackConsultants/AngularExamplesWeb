import { ShowWhenEvenDirective } from './show-when-even.directive';

describe('ShowWhenEvenDirective', () => {
  it('should create an instance', () => {
    // const directive = new ShowWhenEvenDirective(null);
    // expect(directive).toBeTruthy();
  });
});
